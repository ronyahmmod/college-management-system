const processMfsPayment = async ({ amount, paymentMethod }) => {
  // Placeholder: Implement MFS payment logic (bkash, nagad, rocket) letter
  return { transactionId: "TXN123456", status: "completed" };
};

module.exports = { processMfsPayment };
